<!DOCTYPE html>
<html>
    <head>
        <title><%= default_title(yield(:title)) %></title>
        <%= stylesheet_link_tag 'application', media: 'all',
        'data-turbolinks-track' => true
        %>
        <%= javascript_include_tag 'application',
        'data-turbolinks-track' => true
        %>
        <%= csrf_meta_tags %>
        <%= render 'layouts/shim' %>
    </head>
    <body>
        <%= render 'layouts/header' %>
        <div id="main-content">
            <div class="container container-fluid">
               <div class="row notifications">
                <% flash.each do |message_type, message| %>
                    <%= content_tag(:div, message, class: "alert alert-#{message_type}", role: 'alert') %>
                <% end %>
               </div>
                <div class="row">
                    <aside class="col-md-5">
                        <section class="search">
                            <h1>Where would you like to see a show?</h1>
                            <%= form_for :show_search, layout: :horizontal do |f| %>
                                <%= f.text_field :country, placeholder: '' %>
                                <%= f.text_field :state, placeholder: '' %>
                                <%= f.text_field :city, placeholder: '' %>
                                <%= f.submit 'Search', class: 'pull-right' %>
                            <% end %>
                        </section>
                        <section class="price">
                            <h2>Price Range</h2>
                            <input id="price_range" type="text" "span2" value="" data-slider-min="10" data-slider-max="1000" data-slider-step="5" data-slider-value="[20,800]"/>
                        </section>
                        <section class="filters">
                            <h2>Filter Search</h2>
                            <%= form_for :filters, layout: :basic do |f| %>
                                <%= f.check_box :price, label: 'Guaranteed Price Only' %>
                                <%= f.check_box :price, label: 'Tonight' %>
                                <%= f.check_box :price, label: 'Tomorrow' %>
                                <%= f.check_box :price, label: 'This Weekend' %>
                                <%= f.check_box :price, label: 'Next Week' %>
                                <%= f.check_box :price, label: 'Matinees Only' %>
                                <%= f.check_box :price, label: 'Late Night Only' %>
                                <%= f.check_box :price, label: 'With Reviews' %>
                                <%= f.check_box :price, label: 'User Favorites' %>
                                <%= f.check_box :price, label: '1000+ Seats' %>
                                <%= f.check_box :price, label: '-1000 Seats' %>
                                <%= f.check_box :price, label: 'Black Box (Under 50 Seats)' %>
                            <% end %>
                        </section>
                    </aside>
                    <div class="col-md-7 content-area">
                        <%= yield %>
                    </div>
                </div>
                <%= render 'layouts/footer' %>
            </div>
        </div>
    </body>
</html>
