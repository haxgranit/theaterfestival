!!!
%html
  %head
    %meta{content: "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title= default_title(yield(:title))
    = stylesheet_link_tag 'application', media: 'all', data: { turbolinks_track: true }
    = javascript_include_tag 'application', data: { turbolinks_track: true }
    <script src="//cdn.jsdelivr.net/velocity/1.2.3/velocity.min.js"></script>
    = csrf_meta_tags
    = render 'layouts/shim'
    - if Rails.env.production?
      <script type="text/javascript">
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-100109965-1', 'auto');
      </script>
  %body
    = render 'layouts/header'
    .row.alert.alert-warning.justify-content-md-center.text-center
      .col
        %h3 Under Construction
        %p TheaterEngine is still in development. Please be patient with any glitches you encounter as we continue to improve functionality.
        %p
          %strong Thank you! - TheaterEngine Team
    #main-content
      = render 'header_image'
      .container.container-fluid
        .row
          %aside.col-md-4.sidebar-area
            = render 'sidebar'
          .col-md-8.content-area
            - unless current_page?(root_path)
              .row
                = mail_to 'support@theaterengine.com', 'See something wrong? Click here to email support.', subject: "Issue at #{request.original_fullpath}"
            = yield
      = render 'layouts/footer'
