.nested-fields.row.vertical
  = f.input :publication, as: :string
  = f.input :author, as: :string
  = f.input :link, as: :url
  = f.input :quote
  = link_to_remove_association "Cancel", f, class: 'btn btn-primary'
