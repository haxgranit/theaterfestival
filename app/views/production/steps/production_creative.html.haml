.create-show
  %h1
    Create a Show
  .question
    %p
      Add the names of all members of the Creative Team. For key creative credits, tick the appropriate box.
      Key Credits will show up highlighted alongside general information about your show. All other credits will appear only on the Show Page.

  %table.table.table-striped
    %tbody
      - credits = @production.credits.creative
      = render partial: 'production/steps/credits/listing', locals: { target: p, credits: credits }


= link_to 'New Credit', new_credit_path(creditable_id: @production, creditable_type: 'Production', credit_type: :creative), remote: true, class: 'new-credit-button btn btn-primary'
= link_to 'Done', wizard_path(:production_people), class: 'btn btn-primary'
