.create-show
  %h1
    Create a Show
  .question
    %p
      What information would you like to edit?

  = render partial: 'production/steps/credits/credit_form', locals: { target: :credits, type: 'creative' }
%table.table.table-striped
  %tbody
    - @production.credits.creative.each do |credit|
      - p = credit.creditable
      = render partial: 'production/steps/credits/listing', locals: { target: p, credit: credit }
