%table.table.table-striped
  %tbody
    - @production.showtimes.each do |time|
      = render partial: 'production/steps/showtimes/listing', locals: { time: time }
= render partial: 'production/steps/showtimes/showtime_form'
