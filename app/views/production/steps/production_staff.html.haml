%h2 Staff Credits for #{@production.name}
%p What staff of #{@production.name} would you like to credit along with this production?

%table.table.table-striped
  %tbody
    - credits = @production.credits.staff
    = render partial: 'production/steps/credits/listing', locals: { target: p, credits: credits }


= link_to 'New Credit', new_credit_path(creditable_id: @production, creditable_type: 'Production', credit_type: :staff), remote: true, class: 'new-credit-button btn btn-primary'
= link_to 'Done', wizard_path(:production_people), class: 'btn btn-primary'
