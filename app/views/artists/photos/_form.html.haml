#albums
  = simple_form_for @artist do |f|
    - if user_signed_in?
      .links
        = link_to_add_association 'Add Album', f, :albums, partial: 'artists/photos/album_fields', class: 'btn btn-primary'
      .actions
        = f.button :submit, 'Create Albums'
