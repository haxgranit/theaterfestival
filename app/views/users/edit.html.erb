<% provide(:title, "Edit User #{@user.first_name} #{@user.last_name}") %>

<h1><%= "#{@user.first_name} #{@user.last_name}" %></h1>

<%= render 'form' %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
