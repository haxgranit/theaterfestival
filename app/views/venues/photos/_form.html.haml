#albums
  = simple_form_for @venue do |f|
    - if user_signed_in?
      .links
        = link_to_add_association 'Add Album', f, :albums, partial: 'venues/photos/album_fields', class: 'btn btn-primary'
      .actions
        = f.button :submit, 'Create Albums'
